services:
  - type: web
    name: ausuni
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn ausuni.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: PYTHON_VERSION
        value: 3.11.0